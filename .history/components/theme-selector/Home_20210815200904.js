import React from 'react';

export default function Home(props) {
    return (
        <section
            id="home"
            className={styles['section-container']}
            // ref={homeRef}
        ></section>
    );
}
