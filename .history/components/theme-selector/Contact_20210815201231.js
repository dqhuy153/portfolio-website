import React from 'react';

export default function Contact({ className, ...props }) {
    return (
        <section
            id="contact"
            className={className}
            // ref={homeRef}
        ></section>
    );
}
