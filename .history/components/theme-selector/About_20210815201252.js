import React from 'react';

export default function About({ className, ...props }) {
    return (
        <section
            id="about"
            className={className}
            // ref={homeRef}
        ></section>
    );
}
